services:
  - type: web
    name: sentinelai-backend
    env: docker
    repo: "<github-owner>/<repo>" # replace with your GitHub repo, e.g. ArindamTripathi619/sentinelai
    branch: main
    dockerfilePath: backend/Dockerfile
    plan: free
    healthCheckPath: /api/health
