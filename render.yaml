services:
  - type: web
    name: sentinelai-backend
    env: docker
    repo: ArindamTripathi619/sentinelai
    branch: main
    dockerfilePath: backend/Dockerfile
    plan: free
    healthCheckPath: /api/health
